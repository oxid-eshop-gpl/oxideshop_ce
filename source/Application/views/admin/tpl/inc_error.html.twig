{% block admin_inc_error %}
    {% if is_array(Errors.default) and count(Errors.default)>0 %}
    <div class="errorbox">
        {% for key, oEr in Errors.default %}
            <p>{{ oEr.getOxMessage() }}</p>
        {% endfor %}
    </div>
    {% endif %}
{% endblock %}